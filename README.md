# table-tags-in-html
In HTML, table tags are used to display data in rows and columns. The &lt;table> tag defines the table itself, &lt;tr> creates a table row, &lt;th> defines a header cell, and &lt;td> defines a standard data cell. These tags work together to organize and present structured information clearly on a web page
